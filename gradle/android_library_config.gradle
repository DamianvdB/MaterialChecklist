apply plugin: "com.android.library"

apply from: rootProject.file("gradle/android_common_config.gradle")
apply from: rootProject.file("gradle/android_publish_config.gradle")

android {
    defaultConfig {
        archivesBaseName = POM_ARTIFACT_ID
    }
}